# This is a workflow used to validate the behavior of an action in this repo.
name: "Test Action: create-ci-branch"

on:
  push:
    paths: &paths
      #
      # ----- BEGIN DETECTED DEPENDENCIES -----
      - actions-assert/action.yaml
      - actions-assert/asserts.sh
      - checkout-as-app/action.yaml
      - cleanup-hook/action-main.js
      - cleanup-hook/action-post.js
      - cleanup-hook/action.yaml
      - cleanup-hook/inputs.js
      - create-ci-branch/action.yaml
      - create-ci-branch/cleanup/action.yaml
      - create-ci-branch/cleanup/cleanup.sh
      - .github/workflows/test-action-actions-assert.yaml
      - .github/workflows/test-action-checkout-as-app.yaml
      - .github/workflows/test-action-cleanup-hook.yaml
      - .github/workflows/test-action-create-ci-branch.yaml
      # ----- END DETECTED DEPENDENCIES -----
  pull_request:
    paths: *paths

env:
  TEST_REPO: eth-p/gh-actions-test-repo

jobs:
  test-with-defaults:
    name: "Create CI Branch Works"
    runs-on: ubuntu-latest
    steps:
      - id: action
        uses: $/create-ci-branch
        with:
          ghapp-client-id: ${{ secrets.GHAPP_CLIENT_ID }}
          ghapp-private-key: ${{ secrets.GHAPP_PRIVATE_KEY }}
          repository: ${{ env.TEST_REPO }}
          cleanup-after-job: true

      - name: "Run assertions"
        id: assert
        uses: $/actions-assert
        with:
          outputs: ${{ toJson(steps.action.outputs) }}
          asserts: |-
            - name: "Checks out repo"
              is: [ok, not empty]
              run: 'git rev-parse HEAD'

            - name: "Repo is cloned shallow"
              run: 'git rev-parse --is-shallow-repository'
              is:
                - ok
                - equal: true

            - name: "Branch no exists on remote"
              is: [ok, not empty]
              run: 'git ls-remote origin "refs/heads/${TEST_BRANCH}"'

            - name: "Output 'branch' is not empty"
              output: branch
              is: not empty

            - name: "Output 'banner' is not empty"
              output: banner
              is: not empty

            - name: "Output 'git-name' is not empty"
              output: git-name
              is: not empty

            - name: "Output 'git-email' is not empty"
              output: git-email
              is: not empty

            - name: "Output 'token' is not empty"
              output: token
              is: not empty

            - name: "Sets 'TEST_BRANCH' environment variable"
              var: TEST_BRANCH
              is: not empty

  test-cleanup-setup:
    name: "Cleanup Hook (Setup)"
    runs-on: ubuntu-latest
    outputs:
      branch: ${{ steps.test-branch.outputs.branch }}
    steps:
      - id: test-branch
        uses: $/create-ci-branch
        with:
          ghapp-client-id: ${{ secrets.GHAPP_CLIENT_ID }}
          ghapp-private-key: ${{ secrets.GHAPP_PRIVATE_KEY }}
          repository: ${{ env.TEST_REPO }}
          cleanup-after-job: true

  test-cleanup:
    name: "Cleanup Hook"
    needs: [test-cleanup-setup]
    runs-on: ubuntu-latest
    steps:
      - name: "Checkout Repo"
        id: checkout
        uses: $/checkout-as-app
        with:
          ghapp-client-id: ${{ secrets.GHAPP_CLIENT_ID }}
          ghapp-private-key: ${{ secrets.GHAPP_PRIVATE_KEY }}
          repository: ${{ env.TEST_REPO }}

      - name: "Run assertions"
        id: assert
        uses: $/actions-assert
        env: { TEST_BRANCH: "${{ needs.test-cleanup-setup.outputs.branch }}" }
        with:
          asserts: |-
            - name: "Branch no longer exists on remote"
              is: [ok, empty]
              run: 'git ls-remote origin "refs/heads/${TEST_BRANCH}"'
